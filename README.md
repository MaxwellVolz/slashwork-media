# slashwork-media

Public image hosting for the slashwork Instagram content machine.
PNGs under decks/ are referenced by raw url from the Airtable Copy table
and fetched by the Meta Graph API at publish time. Render output only,
no source, no secrets.
